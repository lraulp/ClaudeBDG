// MetricsPage — implementación completa en siguiente iteración
export function MetricsPage() {
  return (
    <div
      className="flex h-full flex-col"
      style={{ padding: '1.5rem 1rem 1rem 1.5rem' }}
    >
      <h1
        className="font-semibold tracking-tight"
        style={{ fontSize: '1.25rem', color: 'var(--on-surface)' }}
      >
        Métricas
      </h1>
      <p style={{ marginTop: '0.5rem', fontSize: '0.875rem', color: 'var(--on-surface)', opacity: 0.5 }}>
        Dashboard de métricas — próxima iteración.
      </p>
    </div>
  )
}
